const CACHE='cakeshop-v1';const ASSETS=['/','/index.html','/manifest.webmanifest','/src/styles.css'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)))});self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k))))) });self.addEventListener('fetch',e=>{const r=e.request;if(r.destination==='document'||r.url.includes('/__/firebase/')){e.respondWith(fetch(r).then(res=>{const c=res.clone();caches.open(CACHE).then(cc=>cc.put(r,c));return res;}).catch(()=>caches.match(r)));}else{e.respondWith(caches.match(r).then(c=>c||fetch(r)));}});